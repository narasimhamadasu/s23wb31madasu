extends layout

block content
  h1= title
  -let results =["green.jpg","kite.jpg","kite1.jpg","red.jpg","yellow.jpeg"]
  -for(var i=0;i<3; i++) {
    -let rand = Math.floor(Math.random()*5)
    img(src=`images/${results[rand]}`,height="300px",width="300px")
  -}
